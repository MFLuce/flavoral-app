<div class="container-fluid mt-5">
  <div class="row">
    <div class="col-sm-2"></div>
    <div class="col-sm-10"></div>
  </div>

  <div class="row">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        {{#if user}}
         {{#if user.isAdmin}}  
          <h1>Hello {{user.userName}} !</h1>
          <div class="container-fluid mt-5 my-5 border">
          <h2>You have new requests to assign!</h2>

        {{else}}
          <h1>Welcome on our request tool!</h1>
          <p>You can contact us to send an order, ask further information on our
            product or whatever matters to you</p>
         {{/if}}

         {{else}}
         <h1>Welcome on our request tool!</h1>
          <p>You can contact us to send an order, ask further information on our
            product or whatever matters to you</p>
        {{/if}}
      </div>
    </div>
  </div>
</div>